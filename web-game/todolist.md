# OpenClaw MMO - 当前任务清单

**生成时间**: 2026-03-08 12:38
**优先级**: P0（紧急）

---

## 🚨 当前问题

**老大发现**：游戏不能真正玩，无法多人联机

**已修复**：
- ✅ P0 任务完成 - 加载真实地图（azure_town.tmx）
- ✅ MMO 服务器运行 - 端口 3000，已运行 47 小时

**未完成（关键！）**：
- ❌ 前端没有连接 MMO 服务器
- ❌ 看不到其他玩家
- ❌ 无法多人战斗

---

## 📋 本次任务（3 个）

### 任务 1：前端 Socket.IO 连接 🔴 最高优先级

**目标**：让前端连接 MMO 服务器

**步骤**：
1. 在 main.ts 中导入 socket.io-client
2. 创建 Socket.IO 连接（http://localhost:3000）
3. 处理连接/断开事件
4. 显示连接状态（UI 提示）
5. 确保编译通过（npm run build）

**文件**：
- `web-game/src/main.ts`（添加连接代码）
- `web-game/package.json`（已添加 socket.io-client 依赖）

**验收**：
- ✅ 浏览器控制台显示"已连接到 MMO 服务器"
- ✅ 服务器日志显示新连接
- ✅ 无编译错误

---

### 任务 2：玩家实时同步 🔴 高优先级

**目标**：看到其他玩家移动

**步骤**：
1. 发送玩家移动事件（socket.emit('player_move', {x, y})）
2. 接收其他玩家位置（socket.on('players_update', ...)）
3. 渲染其他玩家精灵（在地图上显示）
4. 添加玩家名称标签
5. 处理玩家加入/离开

**文件**：
- `web-game/src/main.ts`（添加同步逻辑）
- 服务器已实现（无需修改）

**验收**：
- ✅ 打开两个浏览器窗口
- ✅ 可以看到对方移动
- ✅ 玩家名称显示正确

---

### 任务 3：多人战斗 UI 🔴 高优先级

**目标**：实时对战界面

**步骤**：
1. 添加"挑战玩家"按钮
2. 发送挑战请求（socket.emit('challenge_player', ...)）
3. 接收挑战邀请（显示确认对话框）
4. 进入战斗界面（复用现有 BattleUI）
5. 实时同步战斗行动

**文件**：
- `web-game/src/main.ts`（添加战斗 UI）
- `web-game/src/engine/BattleUI.ts`（已存在，需集成）

**验收**：
- ✅ 可以向其他玩家发起挑战
- ✅ 对方收到邀请并接受
- ✅ 进入战斗界面
- ✅ 双方看到相同战斗状态

---

## ⚠️ 重要提醒

**必须完成所有 3 个任务**，才能让游戏真正 MMO！

**禁止**：
- ❌ 禁止发送中间进度消息
- ❌ 禁止虚假汇报
- ❌ 禁止跳过验收

**必须**：
- ✅ 必须确保 npm run build 编译通过
- ✅ 必须在浏览器中验证功能
- ✅ 必须完成后才能汇报

---

## 📝 完成通知

完成后运行：
```bash
C:\Users\Administrator\AppData\Roaming\npm\openclaw.cmd system event --text "Done: MMO 功能集成完成 - 全球用户可以联机游戏！" --mode now
```
