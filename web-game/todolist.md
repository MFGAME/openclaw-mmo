# OpenClaw MMO - 当前任务清单

**生成时间**: 2026-03-08 05:26
**优先级**: P1 - 核心功能补充（Week 3-4）

---

## 📋 本次心跳任务（3个）

### 任务 1: 动态碰撞管理（CollisionManager 增强）

**目标**: 实现动态添加/移除碰撞区域

**具体任务**:
- [ ] 在 CollisionManager 中添加 `addCollision(x, y)` 方法
- [ ] 添加 `removeCollision(x, y)` 方法
- [ ] 支持临时碰撞（带过期时间）
- [ ] 支持碰撞区域（矩形/圆形）
- [ ] 更新 NPC 和玩家碰撞检测逻辑

**参考**: Tuxemon 的 `collision_manager.py`

**验收标准**:
- 可以动态添加/移除碰撞
- NPC 和玩家能正确响应动态碰撞
- 不影响现有碰撞系统

---

### 任务 2: 事件系统（EventManager）

**目标**: 实现地图事件触发器和条件判断

**具体任务**:
- [ ] 创建 `EventManager.ts`
- [ ] 定义事件类型（触发器、条件、动作）
- [ ] 实现事件触发器（踩踏、交互、自动）
- [ ] 实现条件判断（物品检查、标志位、NPC 状态）
- [ ] 实现动作执行（传送、对话、给予物品、设置标志位）
- [ ] 集成到 Game.ts 主循环

**参考**: Tuxemon 的 `event_engine.py`

**验收标准**:
- 踩踏触发器能正常工作
- 条件判断能正确执行
- 动作能正确触发（传送、对话等）

---

### 任务 3: 场景切换系统（SceneManager）

**目标**: 实现传送门和进出建筑

**具体任务**:
- [ ] 创建 `SceneManager.ts`
- [ ] 实现场景数据结构（地图、NPC、事件）
- [ ] 实现场景加载逻辑
- [ ] 实现传送门（传送点对象）
- [ ] 实现进出建筑（室内/室外切换）
- [ ] 场景切换动画（淡入淡出）
- [ ] 保存当前场景状态

**参考**: Tuxemon 的 `map_manager.py`

**验收标准**:
- 传送门能正常工作
- 进出建筑能正常切换
- 场景切换有动画过渡
- NPC 和事件状态正确保持

---

## 📊 完成后进度预估

- **Week 3-4**: 85% → **95%** ⬆️
- **总体进度**: ~25% → **~30%** ⬆️

---

## ⚠️ 重要提醒

**执行任务前必须**：
1. 阅读 `TUXEMON_RESOURCES.md`
2. 所有资源使用 Tuxemon 原版
3. 禁止使用占位符、禁止自制素材

**完成通知**：
```bash
C:\Users\Administrator\AppData\Roaming\npm\openclaw.cmd system event --text "Done: [任务摘要]" --mode now
```

---

**下次心跳**: 30分钟后自动执行
